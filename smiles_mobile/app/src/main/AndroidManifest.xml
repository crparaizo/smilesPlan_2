<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smiles_mobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gamificacao"></activity>
        <activity android:name=".planejamento" />
        <activity android:name=".telaInicio" />
        <activity android:name=".transferencia" />
        <activity android:name=".metaFim" />
        <activity android:name=".metaInicio" />
        <activity android:name=".blog" />
        <activity android:name=".perfil" />
        <activity android:name=".menu" />
        <activity android:name=".cadastro" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>